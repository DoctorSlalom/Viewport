DATABASE_URL=postgres://...
SESSION_SECRET=change-me-32-chars-minimum-random
VIEWPORT_ADMIN_TOKEN=change-me-random-hex
# Public URL of the app (used in prototype CSP frame-ancestors).
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Absolute path to the prototypes/ directory. Defaults to ../../prototypes
# relative to packages/app, which is the repo root in the monorepo.
PROTOTYPES_ROOT=
